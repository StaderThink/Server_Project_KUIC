<div class="field">
    @if (Label is string)
    {
        <label class="label">@Label</label>
    }

    @ChildContent
</div>

@code {
    [Parameter]
    public string Label { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
}
